{
  "name": "rt-solutions",
  "version": "1.0.0",
  "description": "RT Solutions project",
  "scripts": {
    "start": "cd api && npm start",
    "install-all": "npm install && cd api && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "dev": "concurrently \"cd api && npm run dev\" \"cd client && npm run dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
